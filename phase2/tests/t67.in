void main () {
    ((a + b) - c) * -b / (- (k || b)) + d;
    a == b || ! a != b;
    tautology = (!(condition >= max) == (condition < max)) == true;
    paradox   = ((condition <= max) != !(condition > max)) != true;
    h = (a - -3) == a + 3;
    // chaneg A/=B to A = A/B
    m = m / (2 +
         id +
         k || b -
         -c /
         (x % y) + (y % x)
        );
}