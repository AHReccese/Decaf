class Person {
    string first_name;
    string last_name;
    int age;

    /*
    TODO: add some initialization code to this poor thing
    */
    void report() {
        if (this.age < 18)
            return;
        print(first_name, " ", last_name); // change Print to print
    }
}

int main() {
    Person p;
    p = new Person;
    p.report(); // undefined behaviour
}
