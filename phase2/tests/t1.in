class Student {
    int age;
    double gpa;

    string first_name;
    string last_name;

    void initialize() {
        age = 18;
        this.gpa = 10.0;
    }

    int getAge() {
        return this.age;
    }

    void setName(string new_name) {
        this.first_name = new_name;
    }

    void report() {
        print(this.first_name, " ", this.last_name); // change Print to print
        print("  Age: ", this.age);  // change Print to print
    }
}

int main() {
    string s;
    Student t;
    t = new Student;
    t.initialize();

    s = readLine(); // change Readline to readline
    t.setName(s);

    return 0;
}